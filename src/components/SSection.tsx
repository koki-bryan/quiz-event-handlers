const SSection = () => {
  return (
    <section>
      <h2 className="text-2xl font-bold mb-4">
        Problem 2: Click-to-Select Items (onClick + arrays)
      </h2>
    </section>
  );
};

export default SSection;
